import {{ project_name|replace('-', '_') }}

def test_import():
    '''Test that the package can be imported.'''
    assert {{ project_name|replace('-', '_') }}.__name__ == "{{ project_name|replace('-', '_') }}"
